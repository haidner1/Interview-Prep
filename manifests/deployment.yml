apiVersion : apps/v1
kind: Deployment
metadata:
  name: go-interview-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app:  go-interview-app
  template:
    metadata:
      labels:
        app: go-interview-app     
    spec:
      containers:
        - name: go-interview-app
          image: gointerview.azurecr.io:latest
          imagePullPolicy: Always
          ports:
          - containerPort: 8080
